import React from 'react';
import { Layout } from '../components/Layout';

export const ConfirmEmail = () => {
    return (
        <Layout>
            <p>ConfirmEmail</p>
        </Layout>
    );
};
