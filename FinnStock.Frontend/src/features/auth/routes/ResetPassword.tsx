import React from 'react';
import { Layout } from '../components/Layout';

export const ResetPassword = () => {
    return (
        <Layout>
            <p>ResetPassword</p>
        </Layout>
    );
};
