import React from 'react';
import { Layout } from '../components/Layout';

export const ForgotPassword = () => {
    return (
        <Layout>
            <p>ForgotPassword</p>
        </Layout>
    );
};
